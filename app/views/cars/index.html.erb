<h1>This is a list of all the cars</h1>

<p><%= @all_cars.each do |car| %></p>
			<a href="/cars/{:id}">
				<%= car.year %>
			  <%= car.make %>
	 		  <%= car.model %>
				<%= car.color %>
			</a>
		<% end %>

